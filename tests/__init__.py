"""ParselyFi test package.

Holds the eval harness (``tests/eval``) and any future unit/scenario tests.
Import-safe: contains no top-level heavy imports so ``import tests`` never
pulls in Streamlit or network SDKs.
"""
